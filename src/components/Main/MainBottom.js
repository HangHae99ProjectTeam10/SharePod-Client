import React from "react";
import styled from "styled-components";

const MainBottom = () => {
  return <Wrapper>bottom space</Wrapper>;
};

const Wrapper = styled.div`
  height: 500px;
  // background: pink;
`;

export default MainBottom;
